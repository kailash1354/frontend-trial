/** @type {import('tailwindcss').Config} */
export default {
  darkMode: "class",
  // theme.extend is removed - styles are primarily in theme.css
  theme: {
    // You can keep extend empty or add non-color/font extensions if needed later
    extend: {},
  },
  plugins: [],
};
